{
  "key": "bebok-3.8.1",
  "source": "loadout",
  "version": {
    "supported": "v3"
  },
  "comment": "Build/loadout page (the `full = {...}` SvelteKit var). Separate page from the snapshot - it carries avatar/skin items the snapshot lacks. root = the loadouts array; loadouts[0] is the active build. NOTE: urlTemplate is the support's loadout page; {hash} is the entity loadoutHash. It is left EMPTY by default because the exact lostark.bible loadout URL is unverified - set it to enable loadout fetching (candidate: https://lostark.bible/character/loadout/{hash}). While empty, the worker skips the loadout fetch and these fields are simply not produced (their cells stay blank).",
  "urlTemplate": "",
  "rootPath": "data[2].loadouts",
  "rootPathFallbacks": [
    "data[1].loadouts",
    "data[1].data.loadouts",
    "data[0].loadouts"
  ],
  "refData": [
    "skins"
  ],
  "intermediates": [
    {
      "id": "skinLegend",
      "expr": "new Set(ref.skins&&ref.skins.legend||[])",
      "comment": "Legendary skin item ids (2% main stat each), from data/skins.json"
    },
    {
      "id": "skinEpic",
      "expr": "new Set(ref.skins&&ref.skins.epic||[])",
      "comment": "Epic skin item ids (1% main stat each), from data/skins.json"
    },
    {
      "id": "skinRare",
      "expr": "new Set(ref.skins&&ref.skins.rare||[])",
      "comment": "Rare skin item ids (0.5% main stat each), from data/skins.json"
    },
    {
      "id": "activeLoadout",
      "expr": "root[0]",
      "comment": "loadouts[0] = the active build"
    }
  ],
  "fields": [
    {
      "id": "skinBonusFromLoadout",
      "exprFile": "expr/skinBonusFromLoadout.ts",
      "comment": "Main stat % (fraction, percent-formatted) from skins. Used when the support gear is sourced from a manual character-link override (see fetchCharacterGearPhase); otherwise the manual skinBonus advanced input is used."
    }
  ]
}